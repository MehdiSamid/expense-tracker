import { Routes } from '@angular/router';
import { AddExpenseComponent } from './components/add-expense/add-expense.component';

export const routes: Routes = [{path:"test",component: AddExpenseComponent}];
